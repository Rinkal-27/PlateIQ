# @tensorflow/tfjs-react-native 1.0.0 pins expo-camera@^13, but Expo SDK 51 ships
# expo-camera@15. We don't use tfjs-react-native's optional camera helpers, so the
# stale peer is safe to ignore. This lets `npm install` succeed without --force.
legacy-peer-deps=true
